= simple_form_for(@post) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :text
    - if @post.new_record?
      = f.input :published, input_html: { checked: true }
    = f.input :created_at

  .form-actions
    = f.button :submit
